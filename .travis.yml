language: php

php:
  - 5.5
  - 5.6

install:
  - composer install --no-interaction

before_script:
  - sudo pip install codecov

script:
  - php vendor/bin/phpunit --coverage-clover=coverage.xml -c tests/phpunit.xml

after_success:
  - codecov

notifications:
  email:
    - gulturyan@gmail.com
